import 'package:flutter/material.dart';

class GroupsDemo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Groups demo'),
    );
  }
}
